
# Recommended Python version: 3.11
# Note: PyTorch wheels are distributed separately; install the appropriate wheel for your CUDA version.
# Example:
#   pip install --index-url https://download.pytorch.org/whl/cu126 torch torchvision torchaudio torchmetrics
# CPU-only example:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision torchaudio torchmetrics

# Core Python packages
tqdm>=4.0.0
pandas>=2.2,<2.3
numpy>=1.24,<2
# PyTorch (install wheel using one of the commented commands above)
# Additional (preserved from previous requirements.txt)
kagglehub~=1.0.0
ipywidgets
scikit-learn
matplotlib
joblib
seaborn
pyyaml~=6.0.3

## Install the property-driven-ml package directly from GitHub, the latest version is required for GlobalBounds precondition
# git+https://github.com/tflinkow/property-driven-ml.git
